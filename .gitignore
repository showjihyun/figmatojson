# Build / generated outputs
dist/
output/
extracted/
dashboard/
*.tsbuildinfo
web/dist-client/
web/node_modules/
web/test-results/
web/playwright-report/
web/e2e/_*.png
test-results/

# Local diagnostic / comparison artifacts. tmp-*.mjs are throwaway audit
# scripts kept under web/ so they pick up @playwright/test from
# web/node_modules. The state file holds Figma session cookies — never
# commit. Sibling PNG outputs are regenerable.
web/tmp/
web/tmp-*.mjs

# Dependencies
node_modules/

# Skill installations (regenerable from skills-lock.json)
/skills/
/.agents/

# But keep project-specific skills authored in this repo
.claude/skills/*
!.claude/skills/figma-cli/
!.claude/skills/figma-internals/
!.claude/skills/figma-pen-export/

# Local Claude permissions (user-specific)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local issue tracker workspace (created by /to-prd, /to-issues, etc.)
.scratch/

# Tool-managed local directories
.gstack/
.kiro/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Local env / secrets
.env
.env.local
*.env.local

# Logs / ad-hoc
logs/
*.log
*.tmp

# Local scratch / audit / debug scripts (regenerable, not part of project state)
_tmp_*

# Internal company data — must never be committed (audit data dumps).
# Spec files in docs/specs/audit-*.spec.md are meta-documentation and are kept.
docs/bvp.fig
docs/audit-bvp/
docs/audit-round11/
docs/audit-roundtrip/
docs/audit-raw-coverage/

# Personal sample fixtures.
docs/메타리치 화면 UI Design.fig
docs/메타리치 화면 UI Design.pen
docs/스크린샷*.png
