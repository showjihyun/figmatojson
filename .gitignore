# Build / generated outputs
dist/
output/
extracted/
dashboard/
*.tsbuildinfo
web/dist-client/
web/node_modules/
web/test-results/
web/playwright-report/
web/e2e/_*.png
test-results/

# Dependencies
node_modules/

# Skill installations (regenerable from skills-lock.json)
/skills/
/.agents/

# But keep project-specific skills authored in this repo
.claude/skills/*
!.claude/skills/figma-cli/
!.claude/skills/figma-internals/
!.claude/skills/figma-pen-export/

# Local Claude permissions (user-specific)
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local issue tracker workspace (created by /to-prd, /to-issues, etc.)
.scratch/

# Tool-managed local directories
.gstack/
.kiro/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Local env / secrets
.env
.env.local
*.env.local

# Logs / ad-hoc
logs/
*.log
*.tmp

# Local scratch / audit / debug scripts (regenerable, not part of project state)
_tmp_*

# Audit-round11 LLM-context tiles (regenerable via crop-audit-tiles.mjs)
docs/audit-round11/**/_tiles/

# Round 17 raw-coverage / properties-coverage outputs (measurement only,
# regenerable via audit-raw-coverage.mjs + audit-properties-coverage.mjs).
docs/audit-raw-coverage/
